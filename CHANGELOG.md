# 更新日志 / Changelog

所有显著变更都会记录在此文件 / All notable changes are documented here.

---

## [0.0.29] - 2026-09-26

### 修复 / Fixes
- **HEIC 照片在相册 / 产检报告里显示不出来（重要，影响所有 iPhone 用户）**
  iPhone 默认用 HEIC 格式拍照（部分安卓开启「高效图片格式」后也是）。此前后端只按扩展名放行上传，
  文件能存也能取（接口返回 200），但 **Chrome / Edge / Firefox 都不能解码 HEIC** ——
  浏览器渲染失败后触发 `<img>` 的 error，相册里只显示一张占位图、点开空白，
  而**服务端日志里没有任何报错**，所以一直没被发现。
  现改为：**上传时把 HEIC 转成同目录同名的 .jpg**（纯 JS/WASM，x86 与 ARM 通用）作为显示用文件，
  数据库指向 .jpg，**HEIC 原图保留不删**（删除记录时一并清理）；
  相册、产检照片、产检报告（含从 NAS 导入）三条链路都已覆盖；
  同时启动时**自动回填历史 HEIC 照片**，老照片也能正常显示。
  转码失败不阻断上传：保留原图入库，行为与修复前一致。
- **相册里"打不开的文件"不再默默坏掉**
  - 视频缩略图改用 `<video>` 让浏览器自己渲染首帧（后端本来就不做视频抽帧，
    此前每个视频格子里都是一个碎图标）。
  - 视频 / 图片预览失败时给出**明确说明 + 「下载原文件」入口**。典型场景是 iPhone 录制的
    HEVC / H.265 视频：手机浏览器一般能播，电脑端的 Chrome / Edge 播不了 ——
    现在会直接告诉用户原因和替代办法，而不是给一个黑框。
- **相册网格改用缩略图（手机流量与打开速度）**
  此前相册**没有任何缩略图**，网格里每张都加载全尺寸原图（12MP 约 3~4MB），一屏就是几十 MB。
  现在上传时顺带生成 640px 的 JPEG 缩略图（纯 JS，含 EXIF 方向校正 —— 手机竖拍不会躺倒），
  老照片也会在启动后自动补齐（每次上限 80 张，分几次补完，过程不阻塞使用）。
- **格式支持收口**
  - TIFF 改为**明确拒收**并提示转成 JPEG/PNG —— 浏览器显示不了、PDF 也嵌不进，收下只会变成一个打不开的文件。
  - 相册导出 PDF 时，若原图格式 pdfkit 嵌不进去（WebP / GIF / BMP / AVIF），
    自动**回退用缩略图**，不再留一个「图片无法加载」的空框。
  - `/file`、`/thumbnail` 响应补 `X-Content-Type-Options: nosniff`；SVG 以 `image/svg+xml`
    内联返回时额外加 CSP `sandbox`，堵住「上传的 SVG 在应用同源下执行脚本」这一面。
  - 相册**明确标注支持的格式**：上传对话框里写清「照片 JPG/PNG/WebP/GIF/BMP/HEIC·HEIF
    （HEIC 会自动转成 JPG 保存、原图保留）、视频 MP4/MOV/WebM/M4V」，并提前说明
    iPhone 的 HEVC 视频在电脑浏览器放不出来、TIFF 不支持；相册空状态也补了一行。
- **日记插图补齐两处缺口**
  - **支持 HEIC**：此前只放行 JPEG/PNG/WebP，而 iPhone 默认拍的就是 HEIC ⇒ iPhone 用户在日记里
    **根本插不了图**。现在同样自动转成 JPEG（原图保留）。
  - **纳入备份 / 恢复**：日记插图此前完全没被备份 —— 一旦重装后恢复，日记正文里的图会全部失效
    （正文存的是文件名 URL）。现在备份会带上它们，恢复时按**同名**放回。
  - 顺带：插图类型不符 / 超过 5MB 时给可读的中文提示（此前是「服务器内部错误」）。
- **手机端底部导航栏显示异常（vivo 等机型发虚 / 看似透明）**
  原因有两层：① 导航栏背景为半透明并依赖毛玻璃 `backdrop-filter`，在部分厂商集成的系统 WebView 上不生效；
  ② 本项目此前未声明 `color-scheme`，系统深色模式下 WebView 会对页面做「强制反色」，进一步导致 `position: fixed` 的半透明导航栏渲染异常。
  修复：导航栏改为**固定不透明配色**（不再依赖毛玻璃）；显式声明 `color-scheme: light`（应用内深色主题另有 `dark` 声明）；
  其余依赖毛玻璃的半透明元素统一加 `@supports` 兜底 —— 支持毛玻璃的设备视觉保持不变。

- **全面屏 / Android 15 底部空白与安全区适配**
  此前未声明 `viewport-fit=cover`，导致 `env(safe-area-inset-*)` 恒为 0，
  Android 15（targetSdk 35 强制 edge-to-edge）与全面屏机型上，屏幕底部会露出一条空白
  （系统导航栏透明区，用户描述为「导航栏透明」），导航栏也可能贴住系统手势条。
  修复：补充 `viewport-fit=cover`，同步修正导航栏高度计算（含安全区），并补齐移动端顶部安全区。

- **卸载前抢救照片数据（重要）**
  新增 `cmd/uninstall_init` 逻辑。v0.0.27 及更早版本的照片等用户数据存放在**安装目录**内，
  卸载时会被系统连同目录一起删除；而 `wizard/uninstall` 的「保留数据」选项只能保护持久目录，保护不到它们
  ⇒ 老用户「卸载后重装」会导致相册文件永久丢失（数据库记录仍在，点开图片 404）。
  现在会在卸载前把安装目录中的用户数据复制到持久目录，重装后即可正常显示。
  ⚠️ 该修复只能保护**今后**的卸载操作，此前已丢失的文件无法找回。

- **备份 / 恢复的数据不全，且没有任何提示（重要，影响所有做过备份的用户）**
  陆续修掉四处彼此独立、症状却很像（"恢复了但东西少了/没变"）的问题：
  - **恢复会静默丢字段**：恢复时按一份字段白名单写回数据，白名单里没有的列会被**永久丢掉**
    —— 三围、日记标题、提醒优先级，以及用户改过的**自定义产检日期**（整张表都没进备份范围）。
    备份文件里其实都有，但恢复时用不上。现已补齐，并加了「写真数据 → 备份 → 删掉 → 恢复 → 逐字段核对」的往返测试。
  - **一键恢复漏还原日记插图**：日记正文回来了、**配图全是裂图**（正文里存的是文件名，文件没跟着回来）。
  - **整库恢复被悄悄撤销**：本应用是「内存数据库 + 每 30 秒整库落盘」，原实现只替换文件、不更新内存，
    于是最迟 30 秒后系统又把**恢复前的旧数据**写了回去，而界面却提示"成功，重启后生效"。
    现改为恢复后立即重载内存，**无需重启**。
  - **整库备份可能偏旧**：同一原因，刚写的数据还没落盘就被打包，最多缺 30 秒，且毫无提示。
    现改为备份前先强制落盘。
- **用户填写的推送配置不进备份（企业微信 / 飞书的机器人地址与加签密钥）**
  这两项配置存在数据目录的**文件**里而不在数据库中，此前漏登记 ⇒ **备份里根本没有**；
  即便有也会被误判成应用自带资源、写到安装目录（下次升级就被覆盖）。
  表现就是：升级或换机后，推送不明原因地不发了。现已一并纳入备份与恢复。
  ⚠️ 该修复只对**今后新建的备份**有效，此前已丢失的配置需要重新填一次。
- **推送记录只保留最近一个月；记录列表也不再越滚越长**
  推送记录此前会无限增长：列表一次把全部历史拉回来渲染，页面越滚越长、越用越卡。
  现在：超过 30 天的记录自动清理（应用启动时清一次、之后每天再清一次）；
  列表最多显示最近 300 条并在框内滚动；界面注明「共 N 条 / 仅显示最近 300 条 / 只保留最近 30 天」，
  以防误以为旧记录丢了。
- **产检「标记完成」的几处不顺手**
  - 点完要等后台刷新才变状态（网速慢时像没点上）⇒ 改为**立即变色**，请求失败才回滚。
  - 新增**「取消完成」**：误按可以撤销。⚠️ 它只回退本应用自动生成并标记过的那条记录，
    **用户自己填写的产检数据一律不动**。
  - 撤销后如果该孕周还有**你自己填写的**产检记录，这一项依然会显示为已完成 ——
    现在会明确告诉你就是这个原因（不再出现"提示已取消、状态却没变"的自相矛盾）。
  - 已完成的项目现在能显示**实际完成日期**（由记录推导，不改动任何用户数据）。
- **产检排期改为单一数据源，并按最新规范校正**
  排期此前在后端代码里另有一份副本，与随包的排期表不一致；现统一以随包的排期表为准，
  产检页 / 首页 / 推送 / 导出共用同一份，并对若干检查项的**名称与建议孕周**做了校正。
  已有的产检记录全部保留：完成状态同时按「孕周区间」与「条目标记」两条口径判定，互为兜底，
  不会因为孕周调整而让用户标记过的项目"变回未完成"。
- **早上 8 点前做的备份 / 导出，文件名会写成前一天**
  文件名此前按 UTC 生成 ⇒ 东八区在早上 8 点前导出，会得到前一天的名字，
  同一天做的两个备份挤在一起、分不清先后。现改为按本机时间。
- **移动端日期输入框被截断**（部分窄屏机型看不到完整日期），改为自适应宽度。
- 设置页里较长的提示文字会把页面撑出横向滚动条，现在会自动换行。
- **直接打开「统计」页（或在该页刷新）时一直显示"暂无数据"**
  统计页此前依赖先在别的页面加载过孕期档案；直接进这一页时档案还是空的，于是数据明明都在却显示空列表。
  现在统计面板会自己先取一次档案，直链、刷新、从收藏夹打开都能正常出图。
- **首页导航在其他页面也一直高亮**（切到记录、相册等页面后，首页仍并肩高亮）
  根因是导航高亮用的是「前缀匹配」，而首页路径是 `/` —— 任何页面路径都以它开头，故永远命中。
  现改为精确匹配（子页面显式声明归属，比如从首页进入的「运动指南」）；手机端底部标签栏一并修复。
- **记录页一批图标显示为空白**
  结构性位置改用图形图标之后，一批「内容型」符号（心情、症状、分泌物、服药、运动等）因为不在
  图形字典里而被渲染成空白（记录页 25 个格子里约 20 个空）。现在这类符号按文本正常显示
  （保留原本直观的彩色样式）；只有真正的结构性位置才用图形图标。
- **相册上传对话框在手机上把页面撑宽**
  格式说明与文件选择框挤在同一行里，被压成竖排并顶出屏幕。现改为分行展示、长文自动换行；
  手机端时间线卡片同步收窄（一屏约可见两张照片）。

### 优化 / Improvements
- **数据导出（健康记录 CSV / 日记 PDF / 纪念册 PDF）现在手机端也能下载了**
  此前这三处用的是「前端取回文件再触发下载」，电脑上一直正常，但在手机 App 的内置浏览器里
  常常**点了没反应**、或者提示成功却在下载列表里找不到文件。现在改成直接把下载交给浏览器/系统，
  手机端会走系统的下载流程（文件在下载列表或「文件」应用里）；
  同时**中文文件名不再变成一串 `%E5%AD%95…`**。
  没有可导出内容时仍会先给一句提示，不会让你下载到一个错误页。
- 移动端 `isMobile` 判断在组件挂载时**立即重算一次**，避免个别 WebView 首屏误判为桌面端而渲染出侧边栏。
- **导航栏等结构性位置改用图形图标**（此前是表情符号）：深色模式下部分安卓系统会强行给表情符号改色、
  各家手机画得也不一样，同一处在两部手机上观感不同；图形图标不受系统主题与机型差异影响。
  记录里的食物、心情、症状、体征仍保留表情 —— 那里图形反而不好认。
- 食材搜索支持**口语别名**（输入「西红柿」也能搜到番茄），结果按相关度排序。

### 新增 / Added
- **「记录」里把腰围扩成三围（胸围 / 腰围 / 臀围）**
  三个输入框**可以只填其中一两项**（有的人只量腰围），一项不填才拦；没量的项不会被写成空值冲掉历史。
  统计页里胸围 / 腰围 / 臀围**合并成一张卡片、三条曲线同图对比**
  （此前是三张各自独立的小图，看不出三者的相对走势）；原先的腰围数据全部保留。
  此前的腰围数据全部保留、照常显示和编辑。
- **补上 4 个"数据库里有、界面却没入口"的记录项**
  水肿、阴道分泌物、皮肤状况、排尿情况 —— 这四列本来就在表里，却一直没法录入。
  每项带一句就医提示（如全身瘙痒需排查胆汁淤积、尿痛警惕尿路感染）。
- **饮食内容大扩充**
  - **「能不能吃」食物安全库 393 → 1112 条**（分类 11 → 13 个）：
    新增 **「菌菇类」**（香菇、木耳、金针菇、银耳等 46 种 —— 此前整类都没收录，
    而木耳、香菇恰恰是孕期最常被问的食材之一）与 **「药食同源」**
    （当归、人参、阿胶、益母草、黄芪等 44 种常见药材，孕期能不能吃一目了然）；
    水果（33→123）、肉禽（25→101）、水产（40→117）、蛋奶、谷物、调味、饮品、加工食品等
    也都补齐了日常品种。界面「搜索 1000+ 种食物」的说法现在名副其实。
  - 沿用**按孕期阶段分级**的口径：同一种食物在 **备孕 / 孕早期 / 孕中期 / 孕晚期 / 哺乳期**
    分别给出结论。对生食、酒精、孕期忌用药材等**风险贯穿全程**的条目（如生腌贝类、酒酿、
    益母草、大黄），明确标记为「整个孕期不建议」，而不是只在孕早期提示。
  - **「孕期食谱 / 婴儿喂养」71 → 141 道**：孕早期 7→15、孕中期 12→20、孕晚期 12→20、
    **月子餐 5→20**（原本最薄的一块）、婴儿辅食 19→34、催奶食谱 16→32；
    新增菜谱都写清了「为什么适合这一段」与注意事项（如月子酒的替代、四季豆必须炒熟）。
  - **「今日吃什么」随机池 58 → 73 道**，并修掉一个隐患：旧逻辑是「菜谱少于 30 条才补内置菜谱」，
    一旦数据文件补到 30 条以上，那些经典家常菜会**静默消失**、池子反而比原来更小；
    现改为按菜名去重补齐 —— 文件里有更详细的版本就用文件里的，没有才补内置版。
  - 数据仍标注「仅供参考，具体饮食请遵医嘱」，每条都能看到分阶段结论与原因。
- **统计页大改：补上缺失的曲线 + 每张图给出汇总数字**
  - **补上两项"有录入入口、却从来画不出图"的指标**：**胎心率**与**宫缩持续时间**
    （宫缩卡片里同时列出间隔与疼痛程度；间隔是分钟、持续时间是秒，单位不同所以不共用一条 Y 轴）。
  - **每张图标题下方新增一行汇总数字**：最新值、平均值、最小~最大范围、相对首次的变化量；
    血压、血糖这类多曲线的，每条线各给「最新 / 平均」并标出是哪条（收缩压、舒张压、餐后 1h…），
    避免几个数字摆在一起分不清谁是谁。体重额外给一项**孕期增重**（从孕期第一条体重算起，与所选时段无关）。
  - **记录页的「统计」标签与独立的「统计」页改为同一份实现**：此前两处各自写死一份一模一样的代码，
    改一边必漏另一边（本次新增的胎心率、宫缩就是两边都得改）。现在卡片清单、时段切换、汇总数字只有一处，
    以后新增指标不会再出现"记录页有、统计页没有"。
  - 时段在「近7天 / 近30天 / 近90天 / 孕期全部」之间切换**不再重新请求接口**：一次取回全部记录，
    切换在本地完成，切起来更跟手。
- **统计曲线：记录多了以后"看不见 / 看不懂"的几个问题（重要）**
  记录天天填满整个孕期时（实测 241 天），原来的统计图有以下问题，本次一并修掉：
  - **有些图整张是空白的，但下面明明写着"数据列表 (6条)"**
    原因是图表默认只显示**末尾约 20 个点**。像血糖（每 45 天抽一次）、HCG、尿酸（只在产检时测）
    这类稀疏指标，6 个点全被挪到视野之外 ⇒ 图看起来一片空白，**像是数据丢了**（其实一条没少）。
    现改为：**时段档位直接决定取哪一段数据**，默认把所有点都画出来；想细看再用下面的缩放条。
  - **体温、三围、体重被压成一条直线**
    图表的纵轴原来一律从 0 起：体温 36.1~36.7 被画在 0~40 的轴上、三围 87~104 画在 0~120 的轴上，
    趋势完全看不出来。现改为**纵轴自动贴合数据上下界**。
  - **新增可见的缩放条**：记录多时图下方出现一条可拖动的缩放条，手机上也能用
    （此前只有隐藏的双指缩放/滚轮手势，没人知道能缩放，也不知道当前看的是哪一段）。
  - **卡片右上角标出「起止日期 · 多少条」**，一眼看出这张图覆盖了哪一段、共几条记录。
  - **横轴时间标签不再只剩一个**：此前标签间隔按"总点数"计算，一旦只看其中一段就几乎只剩一个标签，
    现在交给图表按当前可见范围自动排布。
  - 顺带修掉：鼠标停在图上滚动时会变成缩放、页面滚不动；时段切换后不再重复请求接口。

---

## [0.0.28] - 2026-09-23

### 严重修复 / Critical Fixes
- **「记录」保存失败** — 日常记录 INSERT 语句的占位符数量与列名数量不一致（50 vs 51），
  SQLite 报 `50 values for 51 columns`，导致**体重 / 血压 / 血糖 / 腰围等所有日常记录都写不进去**，
  而接口仍返回 HTTP 200。现改为「列名数组自动生成占位符 + 数量自检」，此类问题不会再复发。
- **内置知识库丢失** — 修复「食材安全」显示 0 分类、待产包 / 住院 / 月子清单**初始化不出任何条目**、
  菜谱只剩 38 条兜底、产检计划时间轴为空的问题。根因是「随包发布的只读资源」与「可写业务数据」
  共用同一个目录变量，被一并指向了持久化目录。现已拆分为 `ASSETS_DIR`（安装目录，只读）与
  `DATA_DIR`（持久化，可写）。
- **应用白板** — 日记 / 相册 PDF 导出的流回调（`doc.on('end')`）位于 `try/catch` 之外，
  且未监听 `doc.on('error')`。导出大相册时**中途关闭页面**（响应已关闭）或 pdfkit 内部报错，
  都会抛出未捕获异常 → `process.exit(1)` → **整个应用白板**。现已补 `doc.on('error')`
  与回调内 try/catch。
- **备份后无法恢复** — 「备份」会写入应用共享目录，但「恢复」只允许持久化目录，
  导致「备份列表里能看到文件，点恢复却提示超出备份目录范围」。现已统一为同一套目录白名单。

### 修复 / Fixed
- **检验指标详情 / 编辑 / 删除 404** — `lab_result` 路由误写成下划线 `/lab_results/:id`，统一为 `/lab-results/:id`
- **产检建档失败** — `isValidDate` 在东八区把合法日期判为非法（本地时间解析 + UTC 取回导致回退一天），
  曾使 `POST /checkups` 恒报「checkup_date 格式无效」；改为纯年月日 UTC 校验
- **PDF 中文字体** — `.ttc`（字体集合）注册后报 `createSubset is not a function`，导致日记 / 相册 PDF 导出失败；
  改为优先单字体 `.ttf/.otf`，退回 `.ttc` 时传字族名
- **PDF 页脚版本号** — 引用了不存在的 `config.VERSION`，一直显示为 `v0.0.18`
- **企业微信推送「有的推不成功」**（本轮重点排查）— 逐项定位并修复：
  - **推送失败却记成「成功」** — 只要 HTTP 通了就把响应当成功，连「返回 HTML 错误页」也算成功，
    于是日志里全是绿色「成功」而群里根本没消息。现在严格校验 HTTP 状态码 + JSON 可解析 + `errcode === 0`，
    并把企业微信错误码翻译成中文（如 `93000 → Webhook 地址无效或机器人已被删除`）。
  - **失败没有重试** — 网络抖动、超时、频率超限一次就把当天的推送丢掉。现在网络类错误会
    自动重试（1s / 3s / 8s 共 4 次），`45009 频率超限` 也会重试；地址无效这类永久错误则立刻失败不空转。
  - **当天失败后不再补推** — 调度器在**发起推送前**就把「今天已推过」标记写死，失败也没有第二次机会。
    现在只有**推送成功**才算完成；失败后每 5 分钟自动重试，最多 6 次（覆盖 3 小时补推窗口）。
  - **有两个调度器在打架** — 一个是设置页可配的「每日推送时间」，另一个是代码里硬编码的 21:00。
    后者还查询了不存在的 `pregnancy.status` 列（真实列名 `is_active`），**每次都抛错、从未成功推送过**；
    一旦修好还会变成一天推两次。现已移除硬编码的那个，只保留设置页可配的一个。
  - **待办多时消息超长被整条拒收** — 企业微信 text 消息上限 2048 字节，超了直接报错。
    现在按字节自动截断并附说明，保证消息发得出去。
  - **「推送内容」三个勾选框形同虚设** — `产检提醒` / `提醒事项` 的勾选值只存不用，
    用户取消了照样推。现已真正生效，并改名为更准确的「孕期概览 / 产检安排 / 提醒事项」；
    三项全不勾时不再推送并给出明确提示。
  - **推送记录时间差 8 小时** — 时间戳混用 UTC 与本地时间，除显示错误外，
    东八区 08:00 前的「今天是否已推过」判断会算错。现统一为 NAS 本地时间。
  - **保存配置「测试失败就不保存」** — 网络抖一下用户就得重填一遍。现在先保存再测试，
    测试失败只提示「已保存，但测试发送失败」。
  - **重试按钮发错内容** — 点「重试」发出的是日志标题（如「测试消息」）而不是原文。现记录完整正文，重试原样重发。
  - **「清除」按钮其实没清掉** — 传空地址会被当成「地址不能为空」而报错，界面却把状态清空了，
    看着像清了、配置还在，刷新页面就复活。现明确支持「传空地址 = 清除配置」。
  - **再存一次地址会把推送内容勾选重置回全选** — 保存时未传的开关被当成「开」，
    用户取消过「提醒事项」等勾选后，只要再点一次保存就会被悄悄改回全选。现改为未传的字段保持原值。
- **企微推送报「表/列不存在」** — `schedule_dates` 表未建入 schema，且 JOIN 了不存在的列
- **数据导出「提示成功却找不到文件」** — 导出产物写入安装目录（升级即丢、用户不可见），改优先写共享目录；
  前端下载时过早释放 Blob URL 也已修正
- **手机端上传检查报告失败** — 放宽 MIME 判定（HEIC / 空类型 / `octet-stream` 按扩展名兜底），
  上传临时目录改到持久化目录，错误信息透传到前端
- **NAS 目录浏览失败** — 白名单缺少根路径 `/`，前端从根目录进入必然被拒
- **后端故障不再导致白板** — 前端新增错误边界，崩溃时给出「返回首页 / 刷新页面」出口
- **业务数据落盘位置** — 照片 / 报告 / 备份等不再写入安装目录，并附一次性迁移（复制旧文件 + 重写库内路径）
- **日志不再反噬业务** — 补回 `log.api` 方法；日志写盘整体 try/catch；
  日志刷盘与 WebSocket 心跳等定时器回调也加异常保护（定时器内同步抛出同样会终止进程）
- **导出备份的目录未做校验** — `POST /backup` 的 `dir` 参数此前未做任何校验，可被用于向任意路径写文件；
  现限制在「备份落点 / 用户授权目录 / 共享目录 / 存储卷」之内，越权时返回明确的中文操作指引

### 新增 / Added
- 「设置 → 数据备份与恢复」新增**导出备份到指定位置** —— 原有的「立即备份 / 一键恢复」行为完全不变，
  这是额外把完整备份（数据库 + 照片 + 检查报告）另存到你自己的目录：先在
  「飞牛应用中心 → 孕程记 → 设置 → 授权目录」里授权一个文件夹，回到应用内点「选择目录」选中它，
  再点「导出备份」即可，方便同步到别的硬盘或网盘。页面上同时显示默认备份落点，
  未授权时会给出操作引导。
  - 内置**授权目录排查工具**（设置页可展开）：如果在应用中心授权了目录却选不到，
    这里会直接显示系统下发给应用的原始值，一眼分清是「没授权」「系统没下发」还是「应用没重启」。
  - 支持**手动指定导出目录**：填一个绝对路径即可，后端会先做一次真实写入测试，
    写得进去才接受（ACL 本身就是授权凭证），不依赖授权路径是否下发。
- **新增「飞书」推送**，与企业微信推送并列可选（也可以两个都开）：
  - 设置页出现独立的「飞书推送」区块：Webhook 地址、**加签密钥**（可选）、推送开关、
    推送内容（孕期概览 / 产检安排 / 提醒事项）、每日推送时间、保存 / 测试发送 / 清除。
  - 支持飞书自定义机器人三种安全设置：**签名校验**（自动带 `timestamp` + `sign`）、
    **自定义关键词**（关键词填「孕程记」即可，看板正文自带）、**IP 白名单**。
  - 推送引擎改为「多渠道共用一套逻辑」：**同一个调度器**按各渠道自己的时间推送，
    **同一份看板内容**发给所有已启用的渠道；首页「推送消息」按钮一次推到所有渠道。
  - 「推送记录」合并展示两个渠道，带渠道标签，可按渠道筛选；失败记录照旧可重试。
  - 企业微信原有的接口与配置（`wecom.json` / `/wecom/*`）**完全兼容**，老用户无需做任何事。
- 「记录」支持**腰围**记录（录入弹窗 + 预览卡片 + 趋势统计）
- 「日记」支持**富文本排版与图文混排**（加粗 / 斜体 / 标题 / 列表 / 插图，兼容旧纯文本日记）
- 为**宫缩计时器**（含 5-1-1 规律宫缩提醒）与**胎动计数器**补回首页入口 —— 两个页面此前已实现但无任何入口，用户无法进入

### 修复（沿用 0.0.28 早期改动） / Fixed (carried over)
- **应用中心启用失败** — 禁用高危字段 `disable_authorization_path=false`，避免应用中心误判需要 UI 授权变更导致启用被拦截
- **依赖配置补全** — 新增 `ctl_stop=true` 字段，对齐飞牛官方应用规范

### 说明 / Notes
- 本轮另外确认（不影响使用，未改动）：`/pdf/generate` 等 3 个接口前端已定义但无页面调用且后端未实现；
  `/reference/*` 6 个接口的数据文件从未随包发布（消费方组件同样未被引用）；
  前端存在 49 个重构后遗留的未引用组件（不参与打包）。

---

## [0.0.27] - 2026-09-03

### 迁移 / Migration
- **迁移至飞牛 OS 统一网关** — 由 CGI 模式切换为统一网关常驻服务模式，监听 Unix Socket
- 访问路径从 `/cgi/ThirdParty/pregnancyjournal/index.cgi/api/v1` 变更为 `/app/pregnancyjournal/api/v1`
- 由 TCP 端口改为 Unix Socket（`${TRIM_APPDEST}/target/app.sock`）
- 获取 NAS 登录态网关头（X-Trim-Userid / X-Trim-Isadmin / X-Trim-Username）
- 支持桌面 WebSocket 通信通道

### 修复 / Fixed
- **网关配置** — config 添加 `gatewaySocket`、`gatewayPrefix`、`url` 字段，去除 `port`
- **日记心情** — 修复心情保存后 UI 不显示 emoji 的问题
- **相册日期** — 上传改用 HTML5 `<input type="date">`，绕开 Naive UI DatePicker + date-fns v4 兼容性问题
- **相册编辑/删除** — 新增编辑、删除按钮及完整交互（照片日期/分类/描述可修改）
- **相册预览** — 预览弹窗显示孕周、日期、描述等元数据
- **首页提醒时间** — 时间选择器宽度 100→110px，完整显示时间选项
- **产检完成 UI** — 修复点击"标记完成"后不立即显示"已完成"的响应式问题（立即更新本地状态 + 后台同步）
- **启动顺序** — 修复数据库初始化晚于路由加载导致调度器执行报错的问题
- **日志格式** — 修复 daily-record 日志调用缺少分类参数导致尾部输出 `undefined` 的问题
- **BFCache 处理** — 添加 pageshow/pagehide 监听防止浏览器缓存恢复后 WebSocket 断开

### 基础设施 / Infrastructure
- **日志系统** — 重构为结构化 JSON 日志（异步写入、文件轮转、级别过滤、查询/统计 API）
- **FPK 打包** — 使用官方 `fnpack_tool.exe` 打包，输出标准格式（从 866MB tar 降至 74MB）
- **前端错误捕获** — 全局 Vue errorHandler + Promise rejection + window error 监听

---

## [0.0.10] - 2026

### 变更 / Changed
- 重大 UI 重构 — 导航切换优化
- 记录页单列布局 + 独立滚动 + 数据预览
- 添加记录拆分为 10 种独立小弹窗（体重血压血糖等）
- 日历记录点修复
