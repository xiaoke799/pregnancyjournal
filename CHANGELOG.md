# 更新日志 / Changelog

所有显著变更都会记录在此文件 / All notable changes are documented here.

---

## [0.0.28] - 2026-09-23

### 严重修复 / Critical Fixes
- **「记录」保存失败** — 日常记录 INSERT 语句的占位符数量与列名数量不一致（50 vs 51），
  SQLite 报 `50 values for 51 columns`，导致**体重 / 血压 / 血糖 / 腰围等所有日常记录都写不进去**，
  而接口仍返回 HTTP 200。现改为「列名数组自动生成占位符 + 数量自检」，此类问题不会再复发。
- **内置知识库丢失** — 修复「食材安全」显示 0 分类、待产包 / 住院 / 月子清单**初始化不出任何条目**、
  菜谱只剩 38 条兜底、产检计划时间轴为空的问题。根因是「随包发布的只读资源」与「可写业务数据」
  共用同一个目录变量，被一并指向了持久化目录。现已拆分为 `ASSETS_DIR`（安装目录，只读）与
  `DATA_DIR`（持久化，可写）。
- **应用白板** — 日记 / 相册 PDF 导出的流回调（`doc.on('end')`）位于 `try/catch` 之外，
  且未监听 `doc.on('error')`。导出大相册时**中途关闭页面**（响应已关闭）或 pdfkit 内部报错，
  都会抛出未捕获异常 → `process.exit(1)` → **整个应用白板**。现已补 `doc.on('error')`
  与回调内 try/catch。
- **备份后无法恢复** — 「备份」会写入应用共享目录，但「恢复」只允许持久化目录，
  导致「备份列表里能看到文件，点恢复却提示超出备份目录范围」。现已统一为同一套目录白名单。

### 修复 / Fixed
- **检验指标详情 / 编辑 / 删除 404** — `lab_result` 路由误写成下划线 `/lab_results/:id`，统一为 `/lab-results/:id`
- **产检建档失败** — `isValidDate` 在东八区把合法日期判为非法（本地时间解析 + UTC 取回导致回退一天），
  曾使 `POST /checkups` 恒报「checkup_date 格式无效」；改为纯年月日 UTC 校验
- **PDF 中文字体** — `.ttc`（字体集合）注册后报 `createSubset is not a function`，导致日记 / 相册 PDF 导出失败；
  改为优先单字体 `.ttf/.otf`，退回 `.ttc` 时传字族名
- **PDF 页脚版本号** — 引用了不存在的 `config.VERSION`，一直显示为 `v0.0.18`
- **企微推送报「表/列不存在」** — `schedule_dates` 表未建入 schema，且 JOIN 了不存在的列
- **数据导出「提示成功却找不到文件」** — 导出产物写入安装目录（升级即丢、用户不可见），改优先写共享目录；
  前端下载时过早释放 Blob URL 也已修正
- **手机端上传检查报告失败** — 放宽 MIME 判定（HEIC / 空类型 / `octet-stream` 按扩展名兜底），
  上传临时目录改到持久化目录，错误信息透传到前端
- **NAS 目录浏览失败** — 白名单缺少根路径 `/`，前端从根目录进入必然被拒
- **后端故障不再导致白板** — 前端新增错误边界，崩溃时给出「返回首页 / 刷新页面」出口
- **业务数据落盘位置** — 照片 / 报告 / 备份等不再写入安装目录，并附一次性迁移（复制旧文件 + 重写库内路径）
- **日志不再反噬业务** — 补回 `log.api` 方法；日志写盘整体 try/catch；
  日志刷盘与 WebSocket 心跳等定时器回调也加异常保护（定时器内同步抛出同样会终止进程）

### 新增 / Added
- 「记录」支持**腰围**记录（录入弹窗 + 预览卡片 + 趋势统计）
- 「日记」支持**富文本排版与图文混排**（加粗 / 斜体 / 标题 / 列表 / 插图，兼容旧纯文本日记）
- 为**宫缩计时器**（含 5-1-1 规律宫缩提醒）与**胎动计数器**补回首页入口 —— 两个页面此前已实现但无任何入口，用户无法进入

### 修复（沿用 0.0.28 早期改动） / Fixed (carried over)
- **应用中心启用失败** — 禁用高危字段 `disable_authorization_path=false`，避免应用中心误判需要 UI 授权变更导致启用被拦截
- **依赖配置补全** — 新增 `ctl_stop=true` 字段，对齐飞牛官方应用规范

### 说明 / Notes
- 本轮另外确认（不影响使用，未改动）：`/pdf/generate` 等 3 个接口前端已定义但无页面调用且后端未实现；
  `/reference/*` 6 个接口的数据文件从未随包发布（消费方组件同样未被引用）；
  前端存在 49 个重构后遗留的未引用组件（不参与打包）。

---

## [0.0.27] - 2026-09-03

### 迁移 / Migration
- **迁移至飞牛 OS 统一网关** — 由 CGI 模式切换为统一网关常驻服务模式，监听 Unix Socket
- 访问路径从 `/cgi/ThirdParty/pregnancyjournal/index.cgi/api/v1` 变更为 `/app/pregnancyjournal/api/v1`
- 由 TCP 端口改为 Unix Socket（`${TRIM_APPDEST}/target/app.sock`）
- 获取 NAS 登录态网关头（X-Trim-Userid / X-Trim-Isadmin / X-Trim-Username）
- 支持桌面 WebSocket 通信通道

### 修复 / Fixed
- **网关配置** — config 添加 `gatewaySocket`、`gatewayPrefix`、`url` 字段，去除 `port`
- **日记心情** — 修复心情保存后 UI 不显示 emoji 的问题
- **相册日期** — 上传改用 HTML5 `<input type="date">`，绕开 Naive UI DatePicker + date-fns v4 兼容性问题
- **相册编辑/删除** — 新增编辑、删除按钮及完整交互（照片日期/分类/描述可修改）
- **相册预览** — 预览弹窗显示孕周、日期、描述等元数据
- **首页提醒时间** — 时间选择器宽度 100→110px，完整显示时间选项
- **产检完成 UI** — 修复点击"标记完成"后不立即显示"已完成"的响应式问题（立即更新本地状态 + 后台同步）
- **启动顺序** — 修复数据库初始化晚于路由加载导致调度器执行报错的问题
- **日志格式** — 修复 daily-record 日志调用缺少分类参数导致尾部输出 `undefined` 的问题
- **BFCache 处理** — 添加 pageshow/pagehide 监听防止浏览器缓存恢复后 WebSocket 断开

### 基础设施 / Infrastructure
- **日志系统** — 重构为结构化 JSON 日志（异步写入、文件轮转、级别过滤、查询/统计 API）
- **FPK 打包** — 使用官方 `fnpack_tool.exe` 打包，输出标准格式（从 866MB tar 降至 74MB）
- **前端错误捕获** — 全局 Vue errorHandler + Promise rejection + window error 监听

---

## [0.0.10] - 2026

### 变更 / Changed
- 重大 UI 重构 — 导航切换优化
- 记录页单列布局 + 独立滚动 + 数据预览
- 添加记录拆分为 10 种独立小弹窗（体重血压血糖等）
- 日历记录点修复
