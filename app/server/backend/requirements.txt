# 孕程记 - 后端依赖
# 安装: pip install -r requirements.txt

# 核心框架
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
sqlalchemy[asyncio]>=2.0.0
aiosqlite>=0.19.0
pydantic>=2.5.0

# HTTP 客户端（企业微信 API 调用）
httpx>=0.25.0

# 加密库（VAPID 密钥生成 - Web Push 备选方案）
cryptography>=41.0

# Web Push（浏览器推送，可选）
# pywebpush>=1.14.0

# 图片处理
Pillow>=10.0.0
